package strategies.botPlayingStrategies;

import models.Board;
import models.Cell;
import models.CellStatus;
import models.Move;

import java.util.List;

public class HardBotPlayingStrategy implements strategies.botPlayingStrategies.BotPlayingStrategy {
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
